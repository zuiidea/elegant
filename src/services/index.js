import * as article from './article'
import * as provider from './provider'

/**
*
list      GET     /posts
create    POST    /post
query     GET     /post/:id
update    PUT     /post/:id
remove    DELETE  /post/:id
*/

export {
  provider,
  article,
}
